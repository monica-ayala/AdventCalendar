import Wrapper from "../containers/Wrapper"

type Props = {}

export default function Instructions({}: Props) {
  return (
    <Wrapper><div className="flex flex-col items-center justify-center h-screen p-10">
       bromita </div>
    </Wrapper>
  )
}